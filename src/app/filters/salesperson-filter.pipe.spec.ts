import { SalespersonFilterPipe } from './salesperson-filter.pipe';

describe('SalespersonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SalespersonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
